import type { Metadata } from 'next';
import './globals.css';
import { cn } from '@/lib/utils';
import { Toaster } from '@/components/ui/toaster';

export const metadata: Metadata = {
  title: 'Masjid e Aaisha Qadeem',
  description:
    'A spiritual sanctuary for worship, education, and community unity. Explore prayer times, events, and more.',
  keywords:
    'Masjid e Aaisha Qadeem, mosque, prayer times, Islamic community, Ramadan, Qibla finder, donate, events, Hadith',
  icons: {
    icon: '/favicon.ico',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={cn(
          'min-h-screen bg-background font-body antialiased'
        )}
      >
        <div className="relative flex min-h-screen flex-col">
          {children}
        </div>
        <Toaster />
      </body>
    </html>
  );
}
