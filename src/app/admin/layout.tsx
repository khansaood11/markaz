// This is a placeholder layout.
// The main admin layout has been moved to (main)/layout.tsx
// The login layout is now in login/layout.tsx
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
